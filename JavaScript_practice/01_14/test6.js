let x = 5;

if (x >= 5) {
  // 조건식이 참일 때 실행
  console.log(`${x}는 5이상 이다.`);
}

console.log('조건식 결과와 상관없이 실행된다.');


let y = 4;

if (y >= 5) {
  // 조건식이 참일 때 실행
  console.log(`${y}는 5이상 이다.`);
} else {
  // 조건식이 거짓일 때 실행
  console.log(`${y}는 5미만 이다.`);
}

console.log('조건식 결과와 상관없이 실행된다.');
